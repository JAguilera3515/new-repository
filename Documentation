Relavent information Required

Diagram
[Files] <- [VM] <- [Cloud(SSO)] -> [VM]
                |
               [VM]
               
Single-Sign-On LDAP server with networking file server 
For our SSO we will use Kubernetes, as of right now we will install a Kubernetes NFS(Network File System) on a VM 
for the file system storage. 
               
