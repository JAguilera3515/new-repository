Relavent information Required

Diagram
[Vault] <- [VM] <- [Interim Node(Authenticator] -> [VM] <- [Front End Login Page] <-[VM]
                |
               [VM]
 
Install Vault from Docker Hub on VM,
Use an Authenticator image on another VM to implement security and so that users can access their corresponding data
Front End Login Page will be the web server that holds the information which will prompt user to login
 
Ports needed to be open: 
open port 8200 
open port corresponding to Front End Login Page.
               
